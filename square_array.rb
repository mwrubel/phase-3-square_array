def square_array(array)
  # your code here
  
  array.each do |num|
    squared_Array.unshift = num * num
    puts squared_Array
      squared_Array.each do |numbers|
        second_Squared_Array.unshift = numbers * numbers
        puts second_Squared_Array
    end
  end
end
